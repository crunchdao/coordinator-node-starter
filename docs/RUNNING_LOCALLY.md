# Running Locally

Use [Deployment](DEPLOYMENT.md) as the source of truth.

Minimal flow:

```bash
make deploy
```

Then verify:

- http://localhost:8000/healthz
- http://localhost:3000
- http://localhost:8080
